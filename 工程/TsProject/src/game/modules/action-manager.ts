import { GameModule } from '../base/game-module';
import { Action } from '../plugins/action';

export class ActionManager extends GameModule {
  // coreList: L.List<Action>;

  addAction(action: Action) {
    // L.insert()
  }
}
